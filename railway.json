{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install -r requirements.txt && python3 -m uvicorn src.backend.main:app --host 0.0.0.0 --port $PORT"
  }
}
